- name: Install SSH Keys
  authorized_key:
    user: pi
    state: present
    key: "{{ lookup('file', '/home/andrew/.ssh/id_ed25519.pub') }}"

- name: Upgrade packages
  apt:
    update_cache: yes
    upgrade: yes
  become: yes